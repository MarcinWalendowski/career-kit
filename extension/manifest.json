{
  "manifest_version": 3,
  "name": "Career Kit Capture",
  "version": "0.1.0",
  "description": "Capture the job results already on your screen into your local Career Kit workspace. User initiated only, nothing leaves the machine.",

  "permissions": ["activeTab", "scripting", "storage"],

  "host_permissions": ["http://127.0.0.1/*"],

  "action": {
    "default_popup": "popup.html",
    "default_title": "Capture jobs into Career Kit"
  },

  "background": {
    "service_worker": "background.js",
    "type": "module"
  }
}
